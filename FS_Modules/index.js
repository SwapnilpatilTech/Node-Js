


const { findAreaOfCircle, findAreaOfTriangle, findAreaOfSquare } = require("./functions.js");

const result = findAreaOfSquare(5)
const result = findAreaOfTriangle(5)
const result = findAreaOfCircle(5)
console.log(result);